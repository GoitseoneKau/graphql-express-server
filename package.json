{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "web": "npm --workspace frontend start",
    "api": "npm --workspace api start",
    "dev": "run-p api web",
    "build": "npm run build -ws"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
